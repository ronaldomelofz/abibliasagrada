import { DictionaryPanel } from "@/components/dictionary-panel"

export const metadata = { title: "Dicionário bíblico" }

export default function DictionaryPage() {
  return (
    <main>
      <DictionaryPanel />
    </main>
  )
}
